<template>
  <div class="bottom-nav">
    <div class="nav-item" for="tab in tabs" onclick="tab.onclick">
      <text class="nav-icon">{{tab.icon}}</text>
      <text class="nav-label">{{tab.label}}</text>
    </div>
  </div>
</template>

<script>
export default {
  props: {
    tabs: {
      default: () => [
        { icon: '📋', label: '历史' },
        { icon: '🖼️', label: '选图' },
        { icon: '🌟', label: '成长' }
      ]
    }
  }
}
</script>

<style>
  .bottom-nav {
    flex-direction: row;
    justify-content: space-around;
    padding: 12px 0;
    background-color: #1a1a2e;
  }

  .nav-item {
    flex-direction: column;
    align-items: center;
  }

  .nav-icon {
    font-size: 20px;
    margin-bottom: 2px;
  }

  .nav-label {
    font-size: 13px;
    color: #888888;
  }
</style>
