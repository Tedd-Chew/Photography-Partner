<template>
  <div class="bottom-nav">
    <div class="nav-item" onclick="navToGallery">
      <text class="nav-icon">📋</text>
      <text class="nav-label">历史</text>
    </div>
    <div class="nav-item" onclick="navToUpload">
      <text class="nav-icon">🖼️</text>
      <text class="nav-label">选图</text>
    </div>
    <div class="nav-item" onclick="navToCamera">
      <text class="nav-icon">📷</text>
      <text class="nav-label">拍摄</text>
    </div>
  </div>
</template>

<script>
import router from '@system.router'

export default {
  navToGallery() { router.push({ uri: '/Gallery' }) },
  navToUpload() { router.push({ uri: '/Upload' }) },
  navToCamera() { router.push({ uri: '/Camera' }) }
}
</script>

<style>
  .bottom-nav {
    flex-direction: row;
    justify-content: space-around;
    padding: 12px 0;
    background-color: #1a1a2e;
  }
  .nav-item {
    flex-direction: column;
    align-items: center;
  }
  .nav-icon {
    font-size: 20px;
    margin-bottom: 2px;
  }
  .nav-label {
    font-size: 13px;
    color: #888888;
  }
</style>
