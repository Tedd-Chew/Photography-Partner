<import name="st-icon" src="../apex-ui/components/icon/index"></import>

<template>
  <div class="stats-panel">
    <div class="stats-header">
      <div class="header-icon-wrap">
        <st-icon type="stats" size="16" color="#C4A54A"></st-icon>
      </div>
      <text class="stats-title">统计</text>
    </div>

    <div class="stats-row" for="row in rows">
      <div class="stat-item" for="s in row">
        <text class="stat-num">{{s.value}}</text>
        <text class="stat-label">{{s.label}}</text>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  props: {
    rows: {
      default: () => [
        [
          { value: 0, label: '累计分析' },
          { value: 0, label: '平均分' },
          { value: 0, label: '获得勋章' }
        ],
        [
          { value: 0, label: '最高分' },
          { value: 0, label: '总经验' },
          { value: 0, label: '连续打卡' }
        ]
      ]
    }
  }
}
</script>

<style>
  .stats-panel {
    background-color: #F0F4FC;
    border-radius: 12px;
    padding: 16px;
    margin: 0 8px;
    background-color: #E8EFF7;
    box-shadow: 0 1px 6px rgba(0,0,0,0.03), inset 0 1px 2px rgba(0,0,0,0.02);
  }

  .stats-header {
    flex-direction: row;
    align-items: center;
    margin-bottom: 16px;
  }

  .header-icon-wrap {
    width: 30px;
    height: 30px;
    border-radius: 15px;
    background-color: rgba(196, 165, 74, 0.07);
    border: 1px solid rgba(196, 165, 74, 0.12);
    justify-content: center;
    align-items: center;
    margin-right: 10px;
  }

  .stats-title {
    font-size: 28px;
    font-weight: bold;
    color: #595040;
  }

  .stats-row {
    flex-direction: row;
    margin-bottom: 14px;
  }

  .stat-item {
    flex: 1;
    flex-direction: column;
    align-items: center;
  }

  .stat-num {
    font-size: 36px;
    font-weight: bold;
    color: #C4A54A;
    margin-bottom: 2px;
  }

  .stat-label {
    font-size: 22px;
    color: #86909C;
  }
</style>
