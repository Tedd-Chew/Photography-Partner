<import name="mode-icon" src="../apex-ui/components/icon/index"></import>

<template>
  <div class="mode-selector">
    <div class="mode-option {{value === 'shooting' ? 'option-selected' : ''}}" onclick="selectShooting">
      <mode-icon type="camera" size="20" color="{{value === 'shooting' ? '#C4A54A' : '#86909C'}}"></mode-icon>
      <text class="{{value === 'shooting' ? 'mode-name-selected' : 'mode-name'}}">拍摄指导</text>
      <text class="{{value === 'shooting' ? 'mode-desc-selected' : 'mode-desc'}}">推荐相机参数</text>
    </div>

    <div class="mode-divider"></div>

    <div class="mode-option {{value === 'edit' ? 'option-selected' : ''}}" onclick="selectEdit">
      <mode-icon type="build" size="20" color="{{value === 'edit' ? '#9B70D6' : '#86909C'}}"></mode-icon>
      <text class="{{value === 'edit' ? 'mode-name-selected' : 'mode-name'}}">修图建议</text>
      <text class="{{value === 'edit' ? 'mode-desc-selected' : 'mode-desc'}}">后期调色指导</text>
    </div>

    <div class="mode-divider"></div>

    <div class="mode-option {{value === 'score' ? 'option-selected' : ''}}" onclick="selectScore">
      <mode-icon type="star" size="20" color="{{value === 'score' ? '#C4A54A' : '#86909C'}}"></mode-icon>
      <text class="{{value === 'score' ? 'mode-name-selected' : 'mode-name'}}">评分评价</text>
      <text class="{{value === 'score' ? 'mode-desc-selected' : 'mode-desc'}}">五维打分反馈</text>
    </div>
  </div>
</template>

<script>
export default {
  props: {
    value: { default: 'score' }
  },

  selectShooting() { this.$dispatch('change', { value: 'shooting' }) },
  selectEdit() { this.$dispatch('change', { value: 'edit' }) },
  selectScore() { this.$dispatch('change', { value: 'score' }) }
}
</script>

<style>
  .mode-selector {
    flex-direction: row;
    justify-content: space-around;
    align-items: center;
    padding: 12px 0;
    background-color: #F4F8FC;
    border-radius: 16px;
    margin: 0 16px;
    box-shadow: 0 4px 24px rgba(0,0,0,0.05);
    background-image: linear-gradient(180deg, rgba(255,255,255,1) 0%, rgba(252,252,253,1) 100%);
  }

  .mode-option {
    flex-direction: column;
    align-items: center;
    flex: 1;
    padding: 8px 0;
    border-bottom-width: 2px;
    border-bottom-color: transparent;
    transition: border-bottom-color 0.2s ease, opacity 0.15s ease;
  }

  .mode-option:active {
    opacity: 0.65;
  }

  .option-selected {
    border-bottom-color: #C4A54A;
  }

  .mode-icon {
    margin-bottom: 4px;
  }

  .mode-name {
    font-size: 24px;
    color: #86909C;
    margin-bottom: 2px;
  }

  .mode-name-selected {
    font-size: 24px;
    color: #C4A54A;
    font-weight: bold;
    margin-bottom: 2px;
  }

  .mode-desc {
    font-size: 20px;
    color: #4E5969;
  }

  .mode-desc-selected {
    font-size: 20px;
    color: #86909C;
  }

  .mode-divider {
    width: 1px;
    height: 40px;
    background-color: #DCE4F0;
  }
</style>
