<template>
  <div class="bullet-section">
    <div class="bullet-header">
      <text class="bullet-icon">{{icon}}</text>
      <text class="bullet-title">{{title}}</text>
    </div>
    <div class="bullet-item" for="text in items">
      <text class="bullet-marker">•</text>
      <text class="bullet-text">{{text}}</text>
    </div>
  </div>
</template>

<script>
export default {
  props: {
    icon: { default: '📐' },
    title: { default: '' },
    items: { default: () => [] }
  }
}
</script>

<style>
  .bullet-section {
    background-color: #1a1a2e;
    border-radius: 12px;
    padding: 16px;
    margin: 8px 20px;
  }

  .bullet-header {
    flex-direction: row;
    align-items: center;
    margin-bottom: 10px;
  }

  .bullet-icon {
    font-size: 16px;
    margin-right: 6px;
  }

  .bullet-title {
    font-size: 16px;
    font-weight: bold;
    color: #cccccc;
  }

  .bullet-item {
    flex-direction: row;
    align-items: flex-start;
    margin-bottom: 8px;
  }

  .bullet-marker {
    font-size: 14px;
    color: #ffd700;
    margin-right: 8px;
    margin-top: 2px;
  }

  .bullet-text {
    font-size: 14px;
    color: #cccccc;
    flex: 1;
    line-height: 22px;
  }
</style>
