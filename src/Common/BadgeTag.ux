<import name="bt-icon" src="../apex-ui/components/icon/index"></import>

<template>
  <div class="badge-tag">
    <bt-icon type="star" size="14" color="#C4A54A"></bt-icon>
    <text class="badge-tag-name">{{name}}</text>
  </div>
</template>

<script>
export default {
  props: {
    name: { default: '' }
  }
}
</script>

<style>
  .badge-tag {
    flex-direction: row;
    align-items: center;
    padding: 8px 14px;
    border-radius: 8px;
    background-color: #F0F4FC;
    border: 1px solid rgba(196, 165, 74, 0.15);
    margin-right: 8px;
    margin-bottom: 8px;
    transition: border-color 0.2s ease, background-color 0.2s ease, opacity 0.15s ease;
  }

  .badge-tag:active {
    opacity: 0.7;
    border-color: rgba(196, 165, 74, 0.35);
  }

  .badge-tag-icon {
    margin-right: 4px;
  }

  .badge-tag-name {
    font-size: 24px;
    color: #C4A54A;
  }
</style>
