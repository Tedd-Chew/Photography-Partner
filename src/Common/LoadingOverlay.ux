<template>
  <div>
  <div class="loading-overlay" if="{{show}}">
    <text class="loading-spinner">⏳</text>
    <text class="loading-text">{{text}}</text>
    <text class="loading-hint">{{hint}}</text>
  </div>
</div>
</template>

<script>
export default {
  props: {
    show: { default: false },
    text: { default: 'AI 分析中...' },
    hint: { default: '正在识别场景并生成建议' }
  }
}
</script>

<style>
  .loading-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(10, 10, 26, 0.92);
    justify-content: center;
    align-items: center;
    flex-direction: column;
  }

  .loading-spinner {
    font-size: 48px;
    margin-bottom: 16px;
  }

  .loading-text {
    font-size: 18px;
    color: #ffd700;
    font-weight: bold;
    margin-bottom: 8px;
  }

  .loading-hint {
    font-size: 13px;
    color: #888888;
  }
</style>
