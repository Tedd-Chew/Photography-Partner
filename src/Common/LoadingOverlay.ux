<template>
  <div class="loading-overlay" if="{{visible}}">
    <div class="loading-dot"></div>
    <text class="loading-text">{{text}}</text>
    <text class="loading-hint">{{hint}}</text>
  </div>
</template>

<script>
export default {
  props: {
    visible: { default: false },
    text: { default: 'AI 分析中...' },
    hint: { default: '正在识别场景并生成建议' }
  }
}
</script>

<style>
  .loading-overlay {
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background-color: rgba(237,242,248,0.92);
    justify-content: center;
    align-items: center;
    flex-direction: column;
  }

  .loading-dot {
    width: 40px;
    height: 40px;
    border-radius: 20px;
    background-color: #C4A54A;
    margin-bottom: 16px;
  }

  .loading-text {
    font-size: 28px;
    color: #1A1A2E;
    margin-bottom: 8px;
  }

  .loading-hint {
    font-size: 24px;
    color: #86909C;
  }
</style>
