<import name="spinner" src="../apex-ui/components/spin/index"></import>

<template>
  <div class="loading-overlay" if="{{visible}}">
    <spinner size="large" tip="{{text}}" color="#C4A54A"></spinner>
    <text class="loading-hint">{{hint}}</text>
  </div>
</template>

<script>
export default {
  props: {
    visible: { default: false },
    text: { default: 'AI 分析中...' },
    hint: { default: '正在识别场景并生成建议' }
  }
}
</script>

<style>
  .loading-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(237,242,248,0.92);
    justify-content: center;
    align-items: center;
    flex-direction: column;
  }

  .loading-hint {
    font-size: 24px;
    color: #86909C;
    margin-top: 4px;
  }
</style>
