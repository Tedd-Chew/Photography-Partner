<import name="pp-icon" src="../apex-ui/components/icon/index"></import>

<template>
  <div class="param-panel">
    <div class="panel-header">
      <div class="header-icon-wrap">
        <pp-icon type="camera" size="16" color="#C4A54A"></pp-icon>
      </div>
      <text class="panel-title">{{title}}</text>
    </div>

    <div class="param-list">
      <div class="param-row">
        <text class="param-label">快门速度</text>
        <text class="param-value">{{params.shutter || '--'}}</text>
      </div>
      <div class="param-row">
        <text class="param-label">ISO 感光度</text>
        <text class="param-value">{{params.iso || '--'}}</text>
      </div>
      <div class="param-row">
        <text class="param-label">光  圈</text>
        <text class="param-value">{{params.aperture || '--'}}</text>
      </div>
      <div class="param-row">
        <text class="param-label">白平衡</text>
        <text class="param-value">{{params.wb || '--'}}</text>
      </div>
      <div class="param-row">
        <text class="param-label">曝光补偿</text>
        <text class="param-value">{{params.ev || '--'}}</text>
      </div>
      <div class="param-row">
        <text class="param-label">拍摄模式</text>
        <text class="param-value">{{params.drive || '--'}}</text>
      </div>
      <div class="param-row param-row-last">
        <text class="param-label">对  焦</text>
        <text class="param-value param-value-focus">{{params.focus || '--'}}</text>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  props: {
    params: {
      default: () => ({ shutter: null, iso: null, aperture: null, wb: null, ev: null, drive: null, focus: null })
    },
    title: { default: '建议参数' },
    scene: { default: '' }
  }
}
</script>

<style>
  .param-panel {
    flex-direction: column;
    border-radius: 12px;
    padding: 20px 20px 12px 20px;
    margin: 0 8px;
    background-color: #F0F4FC;
    box-shadow: 0 1px 6px rgba(60,80,120,0.04), inset 0 1px 2px rgba(60,80,120,0.03);
  }

  .panel-header {
    flex-direction: row;
    align-items: center;
    margin-bottom: 16px;
  }

  .header-icon-wrap {
    width: 32px;
    height: 32px;
    border-radius: 16px;
    background-color: rgba(196, 165, 74, 0.09);
    border: 1px solid rgba(196, 165, 74, 0.14);
    justify-content: center;
    align-items: center;
    margin-right: 12px;
  }

  .panel-title {
    font-size: 28px;
    font-weight: bold;
    color: #C4A54A;
  }

  .param-list {
    flex-direction: column;
  }

  .param-row {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    padding: 16px 4px;
    border-bottom: 1px solid #DCE4F0;
  }

  .param-row-last {
    border-bottom-width: 0;
    padding-bottom: 10px;
  }

  .param-label {
    font-size: 26px;
    color: #8A8A90;
  }

  .param-value {
    font-size: 32px;
    color: #1D2129;
    font-weight: 600;
  }

  .param-value-focus {
    font-size: 26px;
    font-weight: 600;
    color: #4E5969;
    flex: 1;
    text-align: right;
    max-lines: 2;
  }
</style>
