<!--
  照片历史卡片 — Gallery 列表用
  占位符: {mode} = shooting(蓝)/edit(紫)/score(金)
  占位符: {thumb} = 缩略图路径
  占位符: {text} = 卡片摘要文本
  占位符: {time} = 日期时间
-->
<template>
  <div class="photo-card">
    <div class="card-image">
      <div class="card-thumb-bg">
        <text class="card-thumb-icon">🖼️</text>
      </div>
    </div>

    <div class="card-body">
      <div class="card-tags">
        <div class="card-mode-tag mode-shooting">
          <text class="card-mode-text">📷 拍摄</text>
        </div>
      </div>
      <text class="card-summary">夜景人像 · 快门 1/30s, ISO 800</text>
      <text class="card-time">2026-05-14 15:30</text>
    </div>

    <div class="card-arrow">
      <text class="arrow-text">〉</text>
    </div>
  </div>
</template>

<style>
  .photo-card {
    flex-direction: row;
    align-items: center;
    background-color: #1a1a2e;
    border-radius: 12px;
    padding: 12px;
    margin: 6px 20px;
  }

  .card-image {
    width: 64px;
    height: 64px;
    border-radius: 8px;
    margin-right: 12px;
  }

  .card-thumb-bg {
    width: 64px;
    height: 64px;
    border-radius: 8px;
    background-color: #2a2a3e;
    justify-content: center;
    align-items: center;
  }

  .card-thumb-icon {
    font-size: 28px;
  }

  .card-body {
    flex: 1;
    flex-direction: column;
  }

  .card-tags {
    flex-direction: row;
    margin-bottom: 6px;
  }

  .card-mode-tag {
    padding: 2px 8px;
    border-radius: 4px;
    margin-right: 6px;
  }

  .card-mode-tag.mode-shooting {
    background-color: rgba(77, 150, 255, 0.15);
  }

  .card-mode-tag.mode-edit {
    background-color: rgba(192, 132, 252, 0.15);
  }

  .card-mode-tag.mode-score {
    background-color: rgba(255, 215, 0, 0.15);
  }

  .card-mode-text {
    font-size: 11px;
    color: #4d96ff;
  }

  .card-summary {
    font-size: 14px;
    color: #cccccc;
    margin-bottom: 4px;
    lines: 1;
    text-overflow: ellipsis;
  }

  .card-time {
    font-size: 11px;
    color: #555555;
  }

  .card-arrow {
    padding: 4px;
  }

  .arrow-text {
    font-size: 16px;
    color: #888888;
  }
</style>
